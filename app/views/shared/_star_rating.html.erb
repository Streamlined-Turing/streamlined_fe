  <%= form_with url: "/media/#{media.id}", method: :patch do |f| %>
    <div class="rating">
      <% (1..5).each do |value| %>
        <%= f.radio_button :rating, value, id: "r#{value}#{media.id}", checked: (media.user_rating == value) %>
        <%= label_tag "r#{value}#{media.id}", '' %>
      <% end %>
    </div>
    <br><br>
      <%= f.submit "Save Rating", class: "btn-default btn" %>
      <br>
      <br>
  <% end %>

